from setuptools import setup

setup(name='aistron',
      version='0.0.1',
      author='Minh Tran',
      packages=['aistron'],
      description='An AIS library',
      license='MIT',
      install_requires=[
            'torch',
      ],
)